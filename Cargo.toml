[package]
name = "we-layerd"
version = "0.2.7"
edition = "2021"

[workspace]
members = [
    "crates/we-core",
    "crates/we-renderer-sys",
    "crates/we-renderer",
    "apps/we-gui",
    "xtask",
]
resolver = "2"

[dependencies]
anyhow = "1.0"
ashpd = { version = "0.13.12", default-features = false, features = ["screencast", "tokio"] }
clap = { version = "4.5", features = ["derive"] }
ctrlc = "3.4"
libc = "0.2"
libpulse-binding = "2.30"
pipewire = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.43", features = ["macros", "net", "rt", "sync", "time"] }
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
we-core = { path = "crates/we-core" }
we-renderer = { path = "crates/we-renderer" }
wayland-backend = { version = "0.3", features = ["client_system"] }
wayland-client = "0.31"
wayland-protocols = { version = "0.32", features = ["client", "staging"] }
wayland-protocols-wlr = { version = "0.3", features = ["client"] }
zbus = "5.16"

[profile.release]
strip = "symbols"
